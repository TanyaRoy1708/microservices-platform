apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: order-service-pdb
  namespace: {{ .Release.Namespace }}
  labels:
    app: order-service
    managed-by: helm
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: order-service
